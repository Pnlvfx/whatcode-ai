improve cli instructions for when tsilscale is enabled, point to their docs, instruct to install ios app
also all setups in general.
test all possible configs mixed

improve notifications for errors, if an error occur both error and idle fire almost at the same time, we should decide how to handle it.

READ ALL DOCS AND IMPROVE THEM.

icon / favicons on docs, email, theme, header logo, marketing page

show qr only if there are no user connected or if --qr is explicity passed.

do not send notifications for sub agent events

troubleshotting: mention whatcode reset
